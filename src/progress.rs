// This module is now a no-op for library usage

/// This function is now a no-op for library usage
pub fn print_progress(_name: &str, _curr: u32, _max: u32) {
    // No-op for library usage
    // Clients of the library can implement their own progress indicators if needed
}
